---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install Debian-specific packages
  ansible.builtin.apt:
    name:
      - apt-transport-https
      - ca-certificates
    state: present
