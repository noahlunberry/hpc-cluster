---
- name: Apply common configuration to all nodes
  hosts: cluster
  become: true
  roles:
    - common

- name: Apply compute node configuration
  hosts: compute
  become: true
  roles:
    - compute_node
